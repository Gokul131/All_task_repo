# 🚀 Backend Engineering Internship Tasks

This repository contains all backend tasks completed as part of the internship assignment.  
Each task demonstrates different backend development concepts including API design, authentication, and database operations.

---

## 📌 Task 1: Flask API with Authentication & CRUD

### 🔹 Features:
- User Signup & Login (JWT Authentication)
- Secure API endpoints
- CRUD operations (Create, Read, Update, Delete)
- SQLite database integration

### 🔹 APIs:
- `POST /signup` → Register user
- `POST /login` → Login and get token
- `POST /items` → Create item (Protected)
- `GET /items` → Get all items (Protected)
- `PUT /items/<id>` → Update item (Protected)
- `DELETE /items/<id>` → Delete item (Protected)

---

## 📌 Task 2: (Update based on your work)

### 🔹 Features:
- (Example: API development / validation / business logic)
- (Add what your Task 2 actually does)

---

## 📌 Task 3: (Update based on your work)

### 🔹 Features:
- Modular backend structure
- Separate files for:
  - Database (`database.py`)
  - Models (`models.py`)
  - Authentication (`auth_utils.py`)
  - Main app (`main.py`)

---

## ⚙️ How to Run the Project

### 1️⃣ Clone the repository
```bash
git clone https://github.com/your-username/All_task_repo.git
cd All_task_repo
